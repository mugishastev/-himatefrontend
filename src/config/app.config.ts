import { env } from './env';

export const appConfig = {
    name: env.appName,
    version: '1.0.0',
    description: 'A modern chat application for everyone.',
    links: {
        github: 'https://github.com/himate',
    },
};
